{
  "name": "spritestack",
  "version": "0.1.0",
  "description": "The complete TestOps ecosystem for TestSprite MCP — one command to rule all your tests",
  "private": true,
  "workspaces": [
    "packages/*",
    "dashboard"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/cli",
    "build": "npm run build --workspace=packages/cli && npm run build --workspace=dashboard",
    "dashboard": "npm run dev --workspace=dashboard",
    "test": "npm run test --workspace=packages/cli"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
